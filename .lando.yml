name: lando-laravel-plugin
services:
  node:
    type: node:14
    build:
      - npm install
    scanner: false
    ssl: false
    sslExpose: false
tooling:
  node:
    service: node
  npm:
    service: node
